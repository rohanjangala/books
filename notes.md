I would like to change the structure of the page better without overdoing it.

I would like to change the main page as one show in the picture. You don't need to add any pricing at the moment, but only the page should be more or less like the image. Only add a web link 'Get recommended books' to direct it to the model recommendations.

Secondly, the current main page which directly gives the recommendations should be in another page (maybe /books is a good name). It should be protected and not immediately load.